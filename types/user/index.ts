import { Thumbnail } from "../thumbnail";

export type User = {
    email_address: any;
    id: string;
    clerkId: string;
    email: string;
    credits: number;
    plan: string;
    createdAt: Date;
  };