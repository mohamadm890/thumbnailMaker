import { User, Package, Mountain, Zap, GitCompare } from "lucide-react";

export const promptTypeIcons = {
  person: User,
  object: Package,
  scene: Mountain,
  action: Zap,
  comparison: GitCompare,
};